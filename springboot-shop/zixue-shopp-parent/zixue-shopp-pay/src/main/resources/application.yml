server:
  port: 9505
#  context-path: /member
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9500/eureka/
spring:
  application:
    name: pay-service
  redis:
    host: hadoop114
    password: admin
    port: 6379
    pool:
      max-idle: 100
      min-idle: 1
      max-active: 1000
      max-wait: -1
    #数据库连接信息
  datasource:
        name: test
        url: jdbc:mysql://hadoop114:3306/shop
        username: root
        password: admin
        # 使用druid数据源
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.jdbc.Driver
        filters: stat
        maxActive: 20
        initialSize: 1
        maxWait: 60000
        minIdle: 1
        timeBetweenEvictionRunsMillis: 60000
        minEvictableIdleTimeMillis: 300000
        validationQuery: select 'x'
        testWhileIdle: true
        testOnBorrow: false
        testOnReturn: false
        poolPreparedStatements: true
        maxOpenPreparedStatements: 20

   
  
  